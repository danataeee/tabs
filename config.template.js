// Copy this file to config.js and paste in your own project's values.
// Both are safe to commit and safe to ship in the browser: the anon key is a
// public client key, and Row Level Security governs what it can actually do.
window.APP_CONFIG = {
  SUPABASE_URL: 'https://YOUR-PROJECT-REF.supabase.co',
  SUPABASE_ANON_KEY: 'YOUR-ANON-KEY'
};
